# Diese Datei startet ALLE Services des CO2-Ökosystems
services:
#  # 1. Der NGINX REVERSE PROXY (Zentraler Eingangspunkt)
#  reverse-proxy:
#    image: nginx:latest
#    container_name: reverse-proxy
#    ports:
#      # Der einzige Port, der nach außen freigegeben wird
#      - "8080:80"
#    volumes:
#      # Wir mappen die nginx.conf, die im selben Ordner (/docker) liegt
#      - ./nginx.conf:/etc/nginx/nginx.conf
#    networks:
#      - co2-network
#    # Stellt sicher, dass der Proxy erst startet, wenn die Backends laufen
#    depends_on:
#      - ledger-service
#      - shop-service
#      # ... (alle anderen Backends hier eintragen)

  # 2. LEDGER SERVICE (Spring Boot)
  ledger-service:
    container_name: ledger-service
    # Der Build-Pfad ist relativ von /docker/
    build: ../services/ledger/backend
    ports:
      - "8080:8080"
    networks:
      - co2-network
    # Kein 'ports' nötig, da der Proxy ihn intern anspricht

#  # 3. SHOP SERVICE (Beispiel-Backend)
#  shop-service:
#    container_name: shop-service
#    build: ../services/shop/backend  # ANGENOMMENER PFAD
#    networks:
#      - co2-network
#
#  # 4. WALLET FRONTEND (z.B. Vue)
#  wallet-ui:
#    container_name: wallet-ui
#    build: ../services/wallet/frontend # ANGENOMMENER PFAD
#    networks:
#      - co2-network
#
#  # 5. SHOP FRONTEND (z.B. React)
#  shop-ui:
#    container_name: shop-ui
#    build: ../services/shop/frontend  # ANGENOMMENER PFAD
#    networks:
#      - co2-network
#
#  # 6. HUB FRONTEND (Die Startseite)
#  hub-ui:
#    container_name: hub-ui
#    build: ../services/hub/frontend   # ANGENOMMENER PFAD
#    networks:
#      - co2-network
#
#  # ... (Hier Exchange-Service, Job-Service etc. einfügen) ...

# Das globale Netzwerk, über das alle Container kommunizieren
networks:
  co2-network:
    driver: bridge